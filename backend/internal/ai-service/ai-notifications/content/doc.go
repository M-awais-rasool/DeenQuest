// Package content builds notification copy: prompts, tone, notification-type context, and fallbacks.
// It uses pkg/ollama for HTTP; other AI capabilities should also use pkg/ollama, not duplicate clients.
package content
