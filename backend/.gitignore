build/
uploads/
.env
.env.*
!.env.example
tmp/

# Python virtual environments (runtime, and the separate one used to build the
# Quran model — see whisper-service/requirements-convert.txt)
whisper-service/.venv/
whisper-service/.venv-convert/
**/__pycache__/
*.pyc

whisper-service/models/
# whisper.cpp build artifacts: a 155 MB GGML model and the converter's throwaway
# venv. Both are reproducible from convert-model.sh.
whisper-cpp-service/models/
whisper-cpp-service/.convert/
